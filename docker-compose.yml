services:
  prometheus:
    image: prom/prometheus:v3.10.0
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    ports:
      - "127.0.0.1:9090:9090"
    restart: unless-stopped

  grafana:
    image: grafana/grafana-oss:12.4.1
    depends_on:
      - prometheus
    volumes:
      - ./monitoring/grafana/provisioning/datasources:/etc/grafana/provisioning/datasources:ro
      - ./monitoring/grafana/nanoclaw.json:/var/lib/grafana/dashboards/nanoclaw.json:ro
      - ./monitoring/grafana/import-dashboard.sh:/usr/local/bin/import-dashboard.sh:ro
      - grafana_data:/var/lib/grafana
    ports:
      - "3002:3000"
    environment:
      GF_SECURITY_ADMIN_PASSWORD: ${GF_SECURITY_ADMIN_PASSWORD}
      GF_USERS_ALLOW_SIGN_UP: "false"
      GF_INSTALL_PLUGINS: ""
    entrypoint: ["/bin/sh", "-c", "/usr/local/bin/import-dashboard.sh & /run.sh"]
    restart: unless-stopped

volumes:
  prometheus_data:
  grafana_data:
